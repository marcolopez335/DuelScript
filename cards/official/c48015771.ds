card "Summon Over" {
    id: 48015771
    type: Field Spell

    passive "Effect 3" {
        scope: self
        grant: cannot_be_destroyed by effect
    }

    effect "Effect 1" {
        speed: 1
        mandatory
        resolve {
            place_counter "Summon Counter" 1 on self
        }
    }

    effect "Effect 5" {
        speed: 1
        mandatory
        resolve {
            send (all, card, either controls) to gy
            send (all, card, either controls) to gy
        }
    }
}
