// 珠玉獣－アルゴザウルス
// Animadorned Archosaur
// Transpiled from c98022050.lua

card "Animadorned Archosaur" {
    id: 98022050
    type: Effect Monster
    attribute: LIGHT
    race: Dinosaur
    level: 1
    atk: 0
    def: 0

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned
        on_resolve {
            destroy (1+, monster, you controls, hand)
            add_to_hand (1+, monster, you controls, where { is_monster and level >= 1 }) from hand
            reveal (1+, card, opponent controls)
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        on_resolve {
            destroy (1+, monster, you controls, hand)
            add_to_hand (1+, monster, you controls, where { is_monster and level >= 1 }) from hand
            reveal (1+, card, opponent controls)
        }
    }

}
