// Ｇゴーレム・ペブルドッグ
// G Golem Pebble Dog
// Transpiled from c25273572.lua

card "G Golem Pebble Dog" {
    id: 25273572
    type: Effect Monster
    attribute: EARTH
    race: Cyberse
    level: 2
    atk: 300
    def: 1000

    raw_effect "Effect 1" {
        effect_type: Single + Trigger
        category: ToHand + SpecialSummon + Search
        code: SummonSuccess
        property: Delay
        count_limit: (1, 25273572)
        on_resolve {
            add_to_hand (1, card, you controls) from gy
            special_summon (1, monster) from gy
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
        }
    }

    raw_effect "Effect 2" {
        effect_type: Single + Trigger
        category: ToHand + SpecialSummon + Search
        code: SpecialSummonSuccess
        property: Delay
        count_limit: (1, 25273572)
        on_resolve {
            add_to_hand (1, card, you controls) from gy
            special_summon (1, monster) from gy
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
        }
    }

    raw_effect "Effect 3" {
        effect_type: Single + Trigger
        category: ToHand + Search
        code: ChainEnd
        property: Delay
        count_limit: (1, 0)
        on_resolve {
            add_to_hand (1+, card, you controls) from deck
            reveal (1+, card, opponent controls)
        }
    }

}
