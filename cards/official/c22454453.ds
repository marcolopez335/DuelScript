card "Jar of Generosity" {
    id: 22454453
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            return (all, card, either controls) to deck
            return (all, card, either controls) to deck
        }
    }
}
