// 束ねられし力
// Strength in Unity
// Transpiled from c96239878.lua

card "Strength in Unity" {
    id: 96239878
    type: Continuous Spell

    raw_effect "Effect 1" {
        effect_type: Activate
        code: FreeChain
        on_resolve {
        }
    }

    raw_effect "Effect 2" {
        effect_type: Field
        code: ChangeCode3
        property: IgnoreRange + BothSide
        range: SpellTrapZone
        on_resolve {
        }
    }

    raw_effect "Effect 3" {
        effect_type: Field + Trigger
        category: Banish
        code: SpecialSummonSuccess
        property: CardTarget + Delay
        range: SpellTrapZone
        count_limit: (1, 96239878)
        on_resolve {
            banish (1+, card, either_player controls)
        }
    }

    raw_effect "Effect 4" {
        effect_type: Ignition
        category: ToHand + ToDeck
        property: CardTarget
        range: SpellTrapZone
        count_limit: (1, 0)
        cost {
            send self to gy
        }
        on_resolve {
            add_to_hand (1, card, you controls) from gy
            return (1+, card, either_player controls) to deck shuffle
        }
    }

}
