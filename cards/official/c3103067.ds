card "Attack Guidance Armor" {
    id: 3103067
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        once_per_turn: hard
        mandatory
        trigger: summoned
        resolve {
            destroy (all, card, either controls)
        }
    }

    effect "Effect 2" {
        speed: 2
        once_per_turn: hard
        mandatory
        trigger: summoned
        resolve {
            destroy (all, card, either controls)
        }
    }
}
