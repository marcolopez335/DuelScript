// ＳＲ吹持童子
// Speedroid Fuki-Modoshi Piper
// Transpiled from c50482813.lua

card "Speedroid Fuki-Modoshi Piper" {
    id: 50482813
    type: Effect Monster | Tuner
    attribute: WIND
    race: Machine
    level: 4
    atk: 1000
    def: 1000

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned
        on_resolve {
            reveal (1, card, opponent controls, deck)
            return (1+, monster, you controls, where { is_face_up and attribute == WIND and level >= 3 }) to hand
            reveal (1+, card, opponent controls)
            shuffle_hand
            send (1+, monster, you controls, where { is_face_up and attribute == WIND and level >= 3 }) to gy
            send_to_deck (1+, card, you controls) bottom
            send_to_deck (1+, card, you controls)
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        on_resolve {
            reveal (1, card, opponent controls, deck)
            return (1+, monster, you controls, where { is_face_up and attribute == WIND and level >= 3 }) to hand
            reveal (1+, card, opponent controls)
            shuffle_hand
            send (1+, monster, you controls, where { is_face_up and attribute == WIND and level >= 3 }) to gy
            send_to_deck (1+, card, you controls) bottom
            send_to_deck (1+, card, you controls)
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        damage_step: false
        activate_from: [gy]
        on_activate {
            select (1+, monster, you controls, where { is_face_up and attribute == WIND and level >= 3 }) as target
        }
        cost {
            banish self
        }
        on_resolve {
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
        }
    }

}
