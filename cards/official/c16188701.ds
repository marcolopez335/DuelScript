// レディ・デバッガー
// Lady Debug
// Transpiled from c16188701.lua

card "Lady Debug" {
    id: 16188701
    type: Effect Monster
    attribute: LIGHT
    race: Cyberse
    level: 4
    atk: 1700
    def: 1400

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned
        on_resolve {
            add_to_hand (1+, card, you controls, where { level <= 3 and race == Cyberse }) from deck
            reveal (1+, card, opponent controls)
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        on_resolve {
            add_to_hand (1+, card, you controls, where { level <= 3 and race == Cyberse }) from deck
            reveal (1+, card, opponent controls)
        }
    }

}
