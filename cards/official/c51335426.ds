// バウンドリンク
// Link Bound
// Transpiled from c51335426.lua

card "Link Bound" {
    id: 51335426
    type: Quick-Play Spell

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        damage_step: false
        on_resolve {
            return (1+, card, you controls, where { is_face_up }) to deck shuffle
            draw 1
            return (1+, card, you controls, where { is_face_up }) to deck shuffle
            send_to_deck (1+, card, you controls)
        }
    }

}
