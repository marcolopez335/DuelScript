card "Photon Veil" {
    id: 9354555
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        resolve {
            return (all, card, either controls) to deck
            add_to_hand (all, card, either controls)
        }
    }
}
