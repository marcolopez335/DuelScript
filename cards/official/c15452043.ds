card "Performapal Sword Fish" {
    id: 15452043
    type: Effect Monster
    attribute: WATER
    race: Fish
    level: 2
    atk: 600
    def: 600

    effect "Effect 1" {
        speed: 1
        mandatory
        trigger: summoned
        resolve {
            modify_atk (all, card, opponent controls, from monster_zone, where is_face_up) - 600 until while_face_up
            modify_def (all, card, opponent controls, from monster_zone, where is_face_up) - 600 until while_face_up
        }
    }

    effect "Effect 2" {
        speed: 1
        mandatory
        trigger: summoned by special
        resolve {
            modify_atk (all, card, opponent controls, from monster_zone, where is_face_up) - 600 until while_face_up
            modify_def (all, card, opponent controls, from monster_zone, where is_face_up) - 600 until while_face_up
        }
    }

    effect "Effect 3" {
        speed: 1
        mandatory
        trigger: summoned by special
        resolve {
            modify_atk (all, card, opponent controls, from monster_zone, where is_face_up) - 600 until while_face_up
            modify_def (all, card, opponent controls, from monster_zone, where is_face_up) - 600 until while_face_up
        }
    }
}
