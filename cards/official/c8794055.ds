// 最果ての宇宙
// The Most Distant, Deepest Depths
// Transpiled from c8794055.lua

card "The Most Distant, Deepest Depths" {
    id: 8794055
    type: Field Spell

    effect "Effect 1" {
        speed: spell_speed_1
        on_resolve {
        }
    }

    continuous_effect "Effect 2" {
        scope: self
        grant: cannot_be_destroyed_by_effect
    }

    continuous_effect "Effect 3" {
        scope: field
        grant: cannot_be_used_as_material
    }

    effect "Effect 4" {
        speed: spell_speed_1
        once_per_turn: hard
        activate_from: [field_zone]
        cost {
            banish self
        }
        on_resolve {
            return (1+, monster, you controls, where { is_face_up and race == Fish and is_synchro }) to hand
            reveal (1+, card, opponent controls)
        }
    }

    effect "Effect 5" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        damage_step: false
        trigger: when_summoned
        activate_from: [gy]
        on_resolve {
            banish (1+, monster, you controls, where { is_face_up and race == Fish and is_synchro })
            return (1+, monster, you controls, where { is_face_up and race == Fish and is_synchro }) to hand
            reveal (1+, card, opponent controls)
        }
    }

    effect "Effect 6" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        damage_step: false
        trigger: when_summoned by_special_summon
        activate_from: [gy]
        on_resolve {
            banish (1+, monster, you controls, where { is_face_up and race == Fish and is_synchro })
            return (1+, monster, you controls, where { is_face_up and race == Fish and is_synchro }) to hand
            reveal (1+, card, opponent controls)
        }
    }

}
