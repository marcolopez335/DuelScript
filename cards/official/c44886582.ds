// 超逸融合
// Apex Polymerization
// Transpiled from c44886582.lua

card "Apex Polymerization" {
    id: 44886582
    // FIXME: card not found in CDB — type/stats are placeholders
    type: Effect Monster
    attribute: DARK
    race: Fiend
    level: 1
    atk: 0
    def: 0

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        damage_step: false
        on_activate {
            select (1+, card, either_player controls, where { is_face_up and level >= 1 }) as target
        }
        cost {
            pay_lp 2000
        }
        on_resolve {
            special_summon (1, monster) from gy
            send (1+, card, either_player controls, where { is_face_up and level >= 1 }) to gy
        }
    }

}
