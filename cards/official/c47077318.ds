card "Sylvan Mikorange" {
    id: 47077318
    type: Effect Monster
    attribute: WATER
    race: Plant
    level: 3
    atk: 400
    def: 1100

    effect "Effect 1" {
        speed: 1
        timing: when
        trigger: custom "chain_end"
        resolve {
            send (all, card, either controls) to gy
        }
    }

    effect "Effect 2" {
        speed: 1
        mandatory
        trigger: custom "chain_end"
        resolve {
            modify_atk (all, card, you control, from monster_zone, where is_face_up and race == Plant) + 300 until while_face_up
            modify_def (all, card, you control, from monster_zone, where is_face_up and race == Plant) + 300 until while_face_up
        }
    }
}
