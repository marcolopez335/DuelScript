// ブランチ
// Branch!
// Transpiled from c30548775.lua

card "Branch!" {
    id: 30548775
    type: Continuous Spell

    effect "Effect 1" {
        speed: spell_speed_1
        on_resolve {
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        optional: true
        timing: when
        damage_step: false
        trigger: on_custom_event "chain_end"
        activate_from: [spell_trap_zone]
        on_activate {
            select (1+, card, either_player controls, where { is_fusion }) as target
        }
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

}
