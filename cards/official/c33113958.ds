// リヴァーチュ・ドラゴン
// LeVirtue Dragon
// Transpiled from c33113958.lua

card "LeVirtue Dragon" {
    id: 33113958
    type: Xyz Monster
    attribute: WATER
    race: Sea Serpent
    rank: 3
    atk: 2000
    def: 0

    summon_condition {
        cannot_normal_summon: true
    }

    materials {
        require: 2+ monster
        same_level: true
        method: xyz
    }

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        on_resolve {
            return (1+, monster, either_player controls, where { card_id == 80534031 }) to hand
            reveal (1+, card, opponent controls)
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: soft
        on_resolve {
            detach 1 overlay_unit from self
            return (1+, monster, either_player controls, where { card_id == 80534031 }) to hand
            reveal (1+, card, opponent controls)
            attach (1, card) to self as_material
            emit_event "custom"
            emit_event "custom"
        }
    }

}
