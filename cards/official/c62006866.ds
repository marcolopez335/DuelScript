// ズバババナイト
// Zubababa Knight
// Transpiled from c62006866.lua

card "Zubababa Knight" {
    id: 62006866
    type: Effect Monster
    attribute: EARTH
    race: Warrior
    level: 3
    atk: 1600
    def: 900

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned
        on_resolve {
            return (1+, monster, opponent controls, where { level <= 4 and is_face_up }) to hand
            reveal (1+, card, opponent controls)
            destroy (1+, monster, opponent controls, where { level <= 4 and is_face_up })
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        on_resolve {
            return (1+, monster, opponent controls, where { level <= 4 and is_face_up }) to hand
            reveal (1+, card, opponent controls)
            destroy (1+, monster, opponent controls, where { level <= 4 and is_face_up })
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        trigger: on_custom_event "chain_end"
        on_resolve {
            return (1+, monster, opponent controls, where { level <= 4 and is_face_up }) to hand
            reveal (1+, card, opponent controls)
        }
    }

    raw_effect "Effect 4" {
        effect_type: Single + Trigger
        category: ToHand + Search
        code: TurnEnd
        property: Delay
        count_limit: (1, 0)
        on_resolve {
            return (1+, monster, opponent controls, where { level <= 4 and is_face_up }) to hand
            reveal (1+, card, opponent controls)
        }
    }

}
