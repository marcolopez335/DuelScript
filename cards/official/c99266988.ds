// 混沌領域
// Chaos Space
// Transpiled from c99266988.lua

card "Chaos Space" {
    id: 99266988
    type: Normal Spell

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        on_resolve {
            add_to_hand (1+, monster, you controls, where { is_monster }) from deck
            reveal (1+, card, opponent controls)
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: soft
        damage_step: false
        activate_from: [gy]
        cost {
            banish self
        }
        on_resolve {
            return (1+, monster, you controls, where { is_monster }) to deck shuffle
            draw 1
        }
    }

}
