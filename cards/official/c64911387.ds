// マテリアクトル・ゼプトウィング
// Materiactor Zeptwing
// Transpiled from c64911387.lua

card "Materiactor Zeptwing" {
    id: 64911387
    type: Effect Monster
    attribute: FIRE
    race: Dragon
    level: 3
    atk: 0
    def: 0

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        activate_from: [hand]
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        trigger: when_summoned
        on_resolve {
            return (1+, card, you controls) to hand
            reveal (1+, card, opponent controls)
            add_to_hand (1, card, you controls) from gy
            special_summon (1, monster) from gy
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        on_resolve {
            return (1+, card, you controls) to hand
            reveal (1+, card, opponent controls)
            add_to_hand (1, card, you controls) from gy
            special_summon (1, monster) from gy
        }
    }

    effect "Effect 4" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        trigger: on_custom_event "chain_active"
        on_resolve {
            return (1+, card, you controls) to hand
            reveal (1+, card, opponent controls)
            add_to_hand (1, card, you controls) from gy
            special_summon (1, monster) from gy
        }
    }

}
