card "Icarus Attack" {
    id: 53567095
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        target (2, card, either controls, from field)
        resolve {
            destroy (all, card, either controls)
        }
    }
}
