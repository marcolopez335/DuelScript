card "Diced Dice" {
    id: 93078761
    type: Quick-Play Spell

    effect "Effect 1" {
        speed: 2
        once_per_turn: hard
        mandatory
        resolve {
            add_to_hand (all, card, either controls)
            add_to_hand (all, card, either controls)
            return (all, card, either controls) to deck
        }
    }
}
