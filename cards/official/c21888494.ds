card "Chosen One" {
    id: 21888494
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        resolve {
            special_summon (all, card, either controls)
            send (all, card, either controls) to gy
            send (all, card, either controls) to gy
        }
    }
}
