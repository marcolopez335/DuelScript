card "Performage Ball Balancer" {
    id: 92530005
    type: Effect Monster | Pendulum Monster
    attribute: EARTH
    race: Spellcaster
    level: 4
    scale: 3
    atk: 1000
    def: 1800

    effect "Effect 1" {
        speed: 1
        once_per_turn: hard
        mandatory
        resolve {
            send (all, card, either controls) to gy
        }
    }

    effect "Effect 2" {
        speed: 1
        once_per_turn: soft
        mandatory
        resolve {
            special_summon (all, card, either controls)
        }
    }

    effect "Effect 3" {
        speed: 1
        once_per_turn: soft
        timing: when
        trigger: summoned
        resolve {
            special_summon (all, card, either controls)
        }
    }

    effect "Effect 4" {
        speed: 1
        once_per_turn: soft
        timing: if
        trigger: summoned
        resolve {
            send (all, card, either controls) to extra_deck
            destroy (all, card, either controls)
        }
    }

    effect "Effect 5" {
        speed: 1
        once_per_turn: soft
        timing: if
        trigger: summoned by special
        resolve {
            send (all, card, either controls) to extra_deck
            destroy (all, card, either controls)
        }
    }
}
