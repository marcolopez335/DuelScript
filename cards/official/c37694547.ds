card "Geartown" {
    id: 37694547
    type: Field Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        condition: reason == destroy
        resolve {
            special_summon (1, card, you control)
            shuffle_deck yours
        }
    }

    effect "Effect 3" {
        speed: 1
        timing: when
        trigger: custom "chain_end"
        condition: reason == destroy
        resolve {
            special_summon (all, card, either controls)
        }
    }
}
