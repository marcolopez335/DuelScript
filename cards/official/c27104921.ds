// 運命の囚人
// Forge a New Future
// Transpiled from c27104921.lua

card "Forge a New Future" {
    id: 27104921
    type: Continuous Trap

    raw_effect "Effect 1" {
        effect_type: Activate
        code: FreeChain
        count_limit: (1, 27104921)
        on_resolve {
        }
    }

    raw_effect "Effect 2" {
        effect_type: Field + Trigger
        category: ToGrave + SpecialSummon + Counter
        code: SpecialSummonSuccess
        property: Delay
        range: SpellTrapZone
        on_resolve {
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
            special_summon (1, monster) from gy
            send (1+, card, you controls, gy) to gy
            special_summon (1, monster) from gy
        }
    }

}
