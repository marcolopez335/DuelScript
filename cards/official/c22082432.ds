// 神の怒り
// Dangers of the Divine
// Transpiled from c22082432.lua

card "Dangers of the Divine" {
    id: 22082432
    type: Quick-Play Spell

    raw_effect "Effect 1" {
        effect_type: Activate
        category: ToHand + SpecialSummon + AtkChange + DefChange
        code: FreeChain
        count_limit: (1, 22082432)
        cost {
            pay_lp 1000
        }
        on_resolve {
            special_summon (1, monster) from gy
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
            destroy (1+, monster, opponent controls)
            return (1+, monster, either_player controls) to hand
        }
    }

    raw_effect "Effect 2" {
        effect_type: Single + MandatoryTrigger
        category: ToGrave
        code: ChainEnd
        count_limit: (1, 0)
        on_resolve {
            send (1+, monster, either_player controls) to gy
        }
    }

    raw_effect "Effect 3" {
        effect_type: Single
        code: SetAttack
        property: SetAvailable + BothSide
        on_resolve {
            modifier: atk + 4000
        }
    }

    raw_effect "Effect 4" {
        effect_type: Single
        code: SetDefense
        property: SetAvailable + BothSide
        on_resolve {
            modifier: def + 4000
        }
    }

    raw_effect "Effect 5" {
        effect_type: Single
        code: 85
        property: SetAvailable + BothSide
        on_resolve {
            register_effect on self { grant: cannot_attack duration: until_end_of_turn }
        }
    }

}
