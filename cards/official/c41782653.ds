// オーバーテクス・ゴアトルス
// Overtex Qoatlus
// Transpiled from c41782653.lua

card "Overtex Qoatlus" {
    id: 41782653
    type: Effect Monster | Spirit
    attribute: DARK
    race: Dinosaur
    level: 7
    atk: 2700
    def: 2100

    summon_condition {
        cannot_normal_summon: true
    }

    raw_effect "Effect 1" {
        effect_type: Single
        code: SpsummonCondition
        property: BothSide + Uncopyable
        on_resolve {
        }
    }

    raw_effect "Effect 2" {
        effect_type: Field
        code: CannotSummon
        property: Uncopyable
        range: Hand
        on_resolve {
            return (1+, monster, either_player controls, where { is_face_up and race == Dinosaur }) to deck shuffle
        }
    }

    effect "Effect 3" {
        speed: spell_speed_2
        once_per_turn: soft
        optional: true
        timing: when
        trigger: opponent_activates [activate_spell | activate_trap | activate_monster_effect]
        on_resolve {
            destroy (1+, monster, either_player controls, where { is_face_up and race == Dinosaur })
            negate activation
            destroy (1+, monster, either_player controls, where { is_face_up and race == Dinosaur })
        }
    }

    effect "Effect 4" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: on_custom_event "chain_end"
        on_resolve {
            add_to_hand (1+, monster, either_player controls, where { is_face_up and race == Dinosaur }) from gy
            reveal (1+, card, opponent controls)
        }
    }

}
