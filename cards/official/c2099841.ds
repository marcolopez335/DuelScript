card "Smile World" {
    id: 2099841
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        resolve {
            modify_atk (all, card, either controls, from monster_zone, where is_face_up) + count((all, card, either controls, from monster_zone)) * 100 until end_of_turn
        }
    }
}
