card "Ninjitsu Art Tool - Iron Digger" {
    id: 32939238
    type: Equip Spell

    effect "Effect 3" {
        speed: 1
        once_per_turn: hard
        mandatory
        cost {
            banish self
        }
        resolve {
            destroy (all, card, either controls)
        }
    }

    effect "Effect 4" {
        speed: 1
        once_per_turn: soft
        timing: if
        trigger: custom "chain_end"
        resolve {
            add_to_hand (all, card, either controls)
            special_summon (all, card, either controls)
        }
    }

    effect "Effect 5" {
        speed: 1
        mandatory
        resolve {
            destroy target
        }
    }
}
