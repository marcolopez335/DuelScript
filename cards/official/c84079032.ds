// 面子蝙蝠
// Bettan Bat
// Transpiled from c84079032.lua

card "Bettan Bat" {
    id: 84079032
    type: Effect Monster | Tuner
    attribute: EARTH
    race: Winged Beast
    level: 1
    atk: 0
    def: 0

    raw_effect "Effect 1" {
        effect_type: Field + Trigger
        category: PositionChange + CoinToss
        code: SummonSuccess
        property: CardTarget + Delay
        range: MonsterZone
        count_limit: (3, 0)
        on_resolve {
            flip_coin { heads { draw 1 } tails { draw 1 } }
            change_battle_position (1, monster)
        }
    }

    raw_effect "Effect 2" {
        effect_type: Field + Trigger
        category: PositionChange + CoinToss
        code: SpecialSummonSuccess
        property: CardTarget + Delay
        range: MonsterZone
        count_limit: (3, 0)
        on_resolve {
            flip_coin { heads { draw 1 } tails { draw 1 } }
            change_battle_position (1, monster)
        }
    }

    raw_effect "Effect 3" {
        effect_type: Field + Trigger
        category: PositionChange
        code: DisableEffect
        property: CardTarget + Delay
        range: MonsterZone
        count_limit: (1, 0)
        on_resolve {
            change_battle_position (1, monster)
        }
    }

}
