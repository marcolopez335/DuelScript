// ライトレイ ソーサラー
// Lightray Sorcerer
// Transpiled from c91349449.lua

card "Lightray Sorcerer" {
    id: 91349449
    type: Effect Monster | Spirit
    attribute: LIGHT
    race: Spellcaster
    level: 6
    atk: 2300
    def: 2000

    summon_condition {
        cannot_normal_summon: true
    }

    raw_effect "Effect 1" {
        effect_type: Single
        code: SpsummonCondition
        property: BothSide + Uncopyable
        on_resolve {
        }
    }

    raw_effect "Effect 2" {
        effect_type: Field
        code: CannotSummon
        property: Uncopyable
        range: Hand
        on_resolve {
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        damage_step: false
        on_activate {
            select (1+, card, either_player controls, where { is_face_up and attribute == LIGHT }) as target
        }
        on_resolve {
            return (1+, card, either_player controls, where { is_face_up and attribute == LIGHT }) to deck shuffle
            banish (1+, card, either_player controls, where { is_face_up and attribute == LIGHT })
        }
    }

}
