// ソード・ライゼオル
// Sword Ryzeal
// Transpiled from c35844557.lua

card "Sword Ryzeal" {
    id: 35844557
    type: Effect Monster
    attribute: FIRE
    race: Thunder
    level: 4
    atk: 1500
    def: 200

    raw_effect "Effect 1" {
        effect_type: Field
        code: CannotSummon
        property: Uncopyable
        range: Hand
        count_limit: (1, 35844557)
        on_resolve {
            register_effect on self {
                grant: cannot_be_destroyed_by_battle
                duration: until_end_of_turn
            }
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        trigger: when_summoned
        on_resolve {
            add_to_hand (1+, monster, you controls, where { attribute == LIGHT and race == Pyro }) from gy
            reveal (1+, card, opponent controls)
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        on_resolve {
            add_to_hand (1+, monster, you controls, where { attribute == LIGHT and race == Pyro }) from gy
            reveal (1+, card, opponent controls)
        }
    }

}
