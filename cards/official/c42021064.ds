// 機動石器ドグラード
// Dogurad, the Stonetrooper
// Transpiled from c42021064.lua

card "Dogurad, the Stonetrooper" {
    id: 42021064
    type: Link Monster
    attribute: EARTH
    race: Rock
    link: 3
    link_arrows: [top, bottom_left, bottom_right]
    atk: 1000

    summon_condition {
        cannot_normal_summon: true
    }

    materials {
        require: 2+ effect monster
        method: link
    }

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        damage_step: false
        trigger: when_summoned by_special_summon
        on_activate {
            select (1+, card, either_player controls, where { race == Rock }) as target
        }
        on_resolve {
        }
    }

    effect "Effect 2" {
        speed: spell_speed_2
        once_per_turn: soft
        optional: true
        damage_step: false
        on_activate {
            select (1+, card, either_player controls, where { race == Rock }) as target
        }
        on_resolve {
            destroy (1+, card, either_player controls, where { race == Rock })
        }
    }

    raw_effect "Effect 3" {
        effect_type: Single
        code: UpdateAttack
        on_resolve {
            modifier: atk + 0
        }
    }

}
