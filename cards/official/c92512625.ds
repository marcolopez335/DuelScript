// 神の忠告
// Solemn Scolding
// Transpiled from c92512625.lua

card "Solemn Scolding" {
    id: 92512625
    type: Normal Trap

    effect "Effect 1" {
        speed: spell_speed_2
        cost {
            pay_lp 3000
        }
        on_resolve {
            negate summon
            destroy (1+, spell, you controls, where { is_face_down })
        }
    }

    effect "Effect 2" {
        speed: spell_speed_2
        cost {
            pay_lp 3000
        }
        on_resolve {
            negate summon
            destroy (1+, spell, you controls, where { is_face_down })
        }
    }

    effect "Effect 3" {
        speed: spell_speed_2
        cost {
            pay_lp 3000
        }
        on_resolve {
            negate summon
            destroy (1+, spell, you controls, where { is_face_down })
        }
    }

    effect "Effect 4" {
        speed: spell_speed_2
        trigger: opponent_activates [activate_spell | activate_trap | activate_monster_effect]
        cost {
            pay_lp 3000
        }
        on_resolve {
            negate activation
            destroy (1+, spell, you controls, where { is_face_down })
        }
    }

}
