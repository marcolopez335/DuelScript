// 射敵
// Shateki
// Transpiled from c5574510.lua

card "Shateki" {
    id: 5574510
    type: Continuous Spell

    raw_effect "Effect 1" {
        effect_type: Activate
        code: FreeChain
        on_resolve {
        }
    }

    raw_effect "Effect 2" {
        effect_type: Ignition
        category: Destroy + ToHand + Search + FusionSummon + LevelChange
        property: CardTarget
        range: SpellTrapZone
        cost {
            discard (1, card, you controls)
        }
        on_resolve {
            flip_coin { heads { draw 1 } tails { draw 1 } }
            destroy (1+, card, you controls, deck)
            add_to_hand (1+, card, you controls) from deck
            reveal (1+, card, opponent controls)
            set_flag "tracked" on self
        }
    }

}
