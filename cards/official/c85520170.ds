// くず鉄の神像
// Scrap-Iron Sacred Statue
// Transpiled from c85520170.lua

card "Scrap-Iron Sacred Statue" {
    id: 85520170
    type: Normal Trap

    effect "Effect 1" {
        speed: spell_speed_2
        once_per_turn: hard
        damage_step: false
        on_resolve {
            special_summon (1, monster) from gy
            return (1+, card, either_player controls, where { race == Dragon and is_synchro and is_face_up }) to deck shuffle
            destroy (1+, monster, opponent controls)
            change_battle_position (1, monster)
            emit_event "custom"
        }
    }

    raw_effect "Effect 2" {
        effect_type: Single + Trigger
        category: SpecialSummon
        code: Destroyed
        property: Delay
        count_limit: (1, 0)
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

}
