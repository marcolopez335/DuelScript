// 光来する奇跡
// Arrive in Light
// Transpiled from c365213.lua

card "Arrive in Light" {
    id: 365213
    type: Continuous Spell

    effect "Effect 1" {
        speed: spell_speed_1
        on_resolve {
            return (1+, card, you controls) to deck shuffle
            shuffle_deck
            reveal (1, card, opponent controls, deck)
        }
    }

    raw_effect "Effect 2" {
        effect_type: Field
        code: IndestructableBattle
        range: SpellTrapZone
        on_resolve {
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        optional: true
        timing: if
        trigger: when_summoned by_special_summon
        activate_from: [spell_trap_zone]
        on_resolve {
        }
    }

}
