// 毒蛇の怨念
// Viper's Grudge
// Transpiled from c1683982.lua

card "Viper's Grudge" {
    id: 1683982
    type: Continuous Trap

    effect "Effect 1" {
        speed: spell_speed_2
        on_resolve {
        }
    }

    continuous_effect "Effect 2" {
        scope: field
        grant: cannot_activate_effects
    }

    continuous_effect "Effect 3" {
        scope: field
        grant: cannot_attack
    }

    effect "Effect 4" {
        speed: spell_speed_2
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_battle_damage
        activate_from: [spell_trap_zone]
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

    effect "Effect 5" {
        speed: spell_speed_2
        once_per_turn: hard
        optional: true
        timing: if
        trigger: on_custom_event "chain_end"
        activate_from: [spell_trap_zone]
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

    raw_effect "Effect 6" {
        effect_type: Field + Trigger
        category: SpecialSummon
        code: FlipSuccess
        property: Oath + Delay
        range: SpellTrapZone
        count_limit: (1, 1683982)
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

    raw_effect "Effect 7" {
        effect_type: Single + Trigger
        category: ToGrave
        code: Destroyed
        property: Delay
        count_limit: (1, 0)
        on_resolve {
            send (1+, card, either_player controls) to gy
        }
    }

}
