// 歌氷麗月
// Kahyoreigetsu
// Transpiled from c39049051.lua

card "Kahyoreigetsu" {
    id: 39049051
    type: Equip Spell

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: soft
        on_resolve {
            special_summon (1, monster) from gy
            equip (1, card) to (1, monster)
            return (1+, spell, either_player controls, where { is_face_up and race == Dragon }) to hand
        }
    }

    raw_effect "Effect 2" {
        effect_type: Single + Continuous
        code: ChainEnd
        property: BothSide
        on_resolve {
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: when
        trigger: during_end_phase
        activate_from: [gy]
        on_resolve {
            return (1+, spell, either_player controls, where { is_face_up and race == Dragon }) to hand
            reveal (1+, card, opponent controls)
        }
    }

}
