// おとり人形
// Bait Doll
// Transpiled from c7165085.lua

card "Bait Doll" {
    id: 7165085
    type: Normal Spell

    effect "Effect 1" {
        speed: spell_speed_1
        on_resolve {
            change_battle_position (1, monster)
            reveal (1+, card, opponent controls)
            destroy (1+, trap, either_player controls, where { is_face_down })
            send (1+, trap, either_player controls, where { is_face_down }) to gy
            reveal (1+, card, opponent controls)
        }
    }

}
