card "Gateway to Chaos" {
    id: 40089744
    type: Field Spell

    effect "Effect 1" {
        speed: 1
        once_per_turn: hard
        mandatory
        resolve {
            add_to_hand (all, card, either controls)
        }
    }

    effect "Effect 3" {
        speed: 1
        once_per_turn: soft
        mandatory
        cost {
            remove_counter "Spell Counter" 3 from self
        }
        resolve {
            add_to_hand (all, card, either controls)
        }
    }
}
