// ナチュルの神星樹
// Naturia Sacred Tree
// Transpiled from c3734202.lua

card "Naturia Sacred Tree" {
    id: 3734202
    type: Continuous Trap

    effect "Effect 1" {
        speed: spell_speed_2
        on_resolve {
        }
    }

    effect "Effect 2" {
        speed: spell_speed_2
        once_per_turn: hard
        optional: true
        activate_from: [spell_trap_zone]
        cost {
            tribute self
        }
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

    effect "Effect 3" {
        speed: spell_speed_2
        once_per_turn: hard
        optional: true
        activate_from: [spell_trap_zone]
        cost {
            tribute self
        }
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

    effect "Effect 4" {
        speed: spell_speed_2
        trigger: on_custom_event "chain_end"
        on_resolve {
            add_to_hand (1+, card, you controls, where { level <= 4 and attribute == EARTH }) from deck
            reveal (1+, card, opponent controls)
        }
    }

}
