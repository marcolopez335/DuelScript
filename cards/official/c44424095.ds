card "Morphtronic Accelerator" {
    id: 44424095
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        target (1, card, either controls, from field)
        resolve {
            return (all, card, either controls) to deck
            destroy (all, card, either controls)
            draw 1
        }
    }
}
