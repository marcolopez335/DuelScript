card "Hero Medal" {
    id: 10489311
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        trigger: custom "chain_end"
        resolve {
            return (all, card, either controls) to deck
            draw 1
        }
    }
}
