// カプシェル
// Capshell
// Transpiled from c76722334.lua

card "Capshell" {
    id: 76722334
    type: Effect Monster
    attribute: DARK
    race: Fiend
    level: 2
    atk: 0
    def: 0

    effect "Effect 1" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: when_tributed
        on_resolve {
            draw 1
        }
    }

    raw_effect "Effect 2" {
        effect_type: Single + Trigger
        category: Draw
        code: FlipSuccess
        property: InitialValue + Delay
        count_limit: (1, 76722334)
        on_resolve {
            draw 1
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: hard
        optional: true
        timing: if
        trigger: on_custom_event "chain_end"
        on_resolve {
            draw 1
        }
    }

    raw_effect "Effect 4" {
        effect_type: Single + Trigger
        category: Draw
        code: TurnEnd
        property: InitialValue + Delay
        count_limit: (1, 76722334)
        on_resolve {
            draw 1
        }
    }

}
