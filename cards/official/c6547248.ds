// 道化の一座『新加入』
// Clown Crew 'New Face'
// Transpiled from c6547248.lua

card "Clown Crew 'New Face'" {
    id: 6547248
    // FIXME: card not found in CDB — type/stats are placeholders
    type: Effect Monster
    attribute: DARK
    race: Fiend
    level: 1
    atk: 0
    def: 0

    effect "Effect 1" {
        speed: spell_speed_1
        on_resolve {
        }
    }

    raw_effect "Effect 2" {
        effect_type: QuickEffect
        category: ToDeck + Draw
        code: FreeChain
        range: SpellTrapZone
        count_limit: (1, 0)
        on_resolve {
            return (1+, spell, you controls) to deck shuffle
            shuffle_deck
            draw 1
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        damage_step: false
        trigger: when_summoned
        activate_from: [spell_trap_zone]
        on_resolve {
            destroy (1+, spell, you controls, gy)
        }
    }

    effect "Effect 4" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: if
        trigger: on_custom_event "chain_end"
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

}
