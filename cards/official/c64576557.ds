card "Triamid Loading" {
    id: 64576557
    type: Continuous Spell

    effect "Effect 1" {
        speed: 1
        mandatory
    }

    effect "Effect 2" {
        speed: 1
        once_per_turn: hard
        timing: if
        trigger: leaves_field
        resolve {
            add_to_hand (all, card, either controls)
        }
    }
}
