// 仇すれば通図
// Way Where There's a Will
// Transpiled from c91880660.lua

card "Way Where There's a Will" {
    id: 91880660
    type: Field Spell

    effect "Effect 1" {
        speed: spell_speed_1
        on_resolve {
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: hard
        activate_from: [field_zone]
        on_resolve {
            reveal (1, card, opponent controls, deck)
            add_to_hand (1+, card, opponent controls) from gy
            reveal (1+, card, opponent controls)
            shuffle_hand
            send (1+, card, opponent controls, gy) to gy
            return (1+, card, opponent controls) to deck shuffle
            send_to_deck (1+, card, you controls) bottom
            send_to_deck (1+, card, you controls)
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        optional: true
        timing: when
        trigger: during_end_phase
        activate_from: [field_zone]
        on_resolve {
            return (1+, card, opponent controls) to deck shuffle
        }
    }

}
