// スターライト・ジャンクション
// Starlight Junktion
// Transpiled from c1003840.lua

card "Starlight Junktion" {
    id: 1003840
    type: Field Spell

    effect "Effect 1" {
        speed: spell_speed_1
        on_resolve {
        }
    }

    effect "Effect 2" {
        speed: spell_speed_1
        once_per_turn: hard
        activate_from: [field_zone]
        cost {
            tribute self
        }
        on_resolve {
            special_summon (1, monster) from gy
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        damage_step: false
        trigger: when_summoned by_special_summon
        activate_from: [field_zone]
        on_activate {
            select (1+, card, you controls, where { is_tuner }) as target
        }
        on_resolve {
            return (1+, card, you controls, where { is_tuner }) to deck shuffle
        }
    }

}
