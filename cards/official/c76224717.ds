card "Advance Zone" {
    id: 76224717
    type: Continuous Spell

    effect "Effect 1" {
        speed: 1
        mandatory
    }

    effect "Effect 2" {
        speed: 1
        once_per_turn: soft
        timing: when
        trigger: end_phase
        resolve {
            destroy (all, card, either controls)
            draw 1
            return (all, card, either controls) to hand
        }
    }
}
