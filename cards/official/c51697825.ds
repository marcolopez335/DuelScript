card "Jack-In-The-Hand" {
    id: 51697825
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        once_per_turn: hard
        mandatory
        resolve {
            add_to_hand (all, card, either controls)
            send (all, card, either controls) to gy
            add_to_hand (all, card, either controls)
            send (all, card, either controls) to gy
        }
    }
}
