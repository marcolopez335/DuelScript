// フォーチュンレディ・ウインディー
// Fortune Lady Wind
// Transpiled from c82693917.lua

card "Fortune Lady Wind" {
    id: 82693917
    type: Effect Monster
    attribute: WIND
    race: Spellcaster
    level: 3
    atk: ?
    def: ?

    raw_effect "Effect 1" {
        effect_type: Single
        code: SetAttack
        property: SingleRange
        range: MonsterZone
        on_resolve {
            modifier: atk + self.level * 300
        }
    }

    raw_effect "Effect 2" {
        effect_type: Single
        code: SetDefense
        property: SingleRange
        range: MonsterZone
        on_resolve {
            modifier: def + self.level * 300
        }
    }

    effect "Effect 3" {
        speed: spell_speed_1
        once_per_turn: soft
        trigger: during_standby_phase
        on_resolve {
        }
    }

    effect "Effect 4" {
        speed: spell_speed_1
        optional: true
        timing: when
        trigger: when_summoned
        on_resolve {
            destroy (1+, spell, opponent controls, where { is_spell })
        }
    }

}
