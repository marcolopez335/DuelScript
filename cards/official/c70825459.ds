card "Trap Tracks" {
    id: 70825459
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        once_per_turn: hard
        mandatory
        target (1, card, you control, from monster_zone)
        resolve {
            destroy (all, card, either controls)
        }
    }
}
