card "Champion's Vigilance" {
    id: 82382815
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }

    effect "Effect 2" {
        speed: 2
        mandatory
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }

    effect "Effect 3" {
        speed: 2
        mandatory
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }

    effect "Effect 4" {
        speed: 2
        mandatory
        trigger: opponent_activates [activate_spell, activate_trap, activate_spell]
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }
}
