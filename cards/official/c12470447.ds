card "Curse of Fiend" {
    id: 12470447
    type: Normal Spell

    passive "Effect 3" {
        scope: self
        grant: cannot_change_position
    }

    effect "Effect 1" {
        speed: 1
        mandatory
        resolve {
            change_position (all, card, either controls, from monster_zone) to defense_position
            grant (all, card, either controls, from monster_zone) cannot_change_position until end_of_turn
        }
    }
}
