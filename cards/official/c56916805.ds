card "Energy Drain" {
    id: 56916805
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            modify_atk target + count((all, card, opponent controls, from hand)) * 200 until end_of_turn
            modify_def target + count((all, card, opponent controls, from hand)) * 200 until end_of_turn
        }
    }
}
