card "Weapon Change" {
    id: 10035717
    type: Continuous Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        cost {
            pay_lp 700
        }
        resolve {
            set_atk target target.def until end_of_turn
            set_def target target.atk until end_of_turn
        }
    }

    effect "Effect 2" {
        speed: 1
        once_per_turn: soft
        timing: when
        trigger: standby_phase
        cost {
            pay_lp 700
        }
        resolve {
            set_atk target target.def until end_of_turn
            set_def target target.atk until end_of_turn
        }
    }
}
