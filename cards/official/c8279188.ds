card "Depth Amulet" {
    id: 8279188
    type: Continuous Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            discard (all, card, either controls)
            negate
        }
    }

    effect "Effect 2" {
        speed: 2
        timing: when
        trigger: summoned
        resolve {
            discard (all, card, either controls)
            negate
        }
    }
}
