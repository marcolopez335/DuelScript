card "Lightning Storm" {
    id: 14532163
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        once_per_turn: hard
        mandatory
        resolve {
            destroy (all, card, either controls)
            destroy (all, card, either controls)
        }
    }
}
