card "Invocation" {
    id: 74063034
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        resolve {
        }
    }

    effect "Effect 2" {
        speed: 1
        once_per_turn: hard
        mandatory
        resolve {
            return (all, card, either controls) to deck
            add_to_hand (all, card, either controls)
        }
    }
}
