card "Fire Hand" {
    id: 68535320
    type: Effect Monster
    attribute: FIRE
    race: Pyro
    level: 4
    atk: 1600
    def: 1000

    effect "Effect 1" {
        speed: 1
        timing: when
        trigger: custom "chain_end"
        resolve {
            destroy (all, card, either controls)
            special_summon (all, card, either controls)
        }
    }
}
