card "Link Bound" {
    id: 51335426
    type: Quick-Play Spell

    effect "Effect 1" {
        speed: 2
        once_per_turn: hard
        mandatory
        resolve {
            return (all, card, either controls) to deck
            draw 1
            return (all, card, either controls) to deck
        }
    }
}
