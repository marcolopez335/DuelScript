card "Synchro Cracker" {
    id: 41097056
    type: Normal Spell

    effect "Effect 1" {
        speed: 1
        mandatory
        resolve {
            return (all, card, either controls) to deck
            destroy (all, card, either controls)
        }
    }
}
