card "Fukubiki" {
    id: 54927180
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            add_to_hand (all, card, either controls)
            send (all, card, either controls) to gy
            add_to_hand (all, card, either controls)
            send (all, card, either controls) to gy
        }
    }
}
