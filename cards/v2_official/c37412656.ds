card "Hero Blast" {
    id: 37412656
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            return (all, card, either controls) to hand
            destroy (all, card, either controls)
        }
    }
}
