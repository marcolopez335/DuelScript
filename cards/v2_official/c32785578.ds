card "Farewelcome Labrynth" {
    id: 32785578
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        trigger: summoned
        resolve {
            negate
            destroy (all, card, either controls)
            destroy (all, card, either controls)
        }
    }
}
