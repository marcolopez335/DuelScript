card "Wattcancel" {
    id: 56993276
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }

    effect "Effect 2" {
        speed: 2
        mandatory
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }
}
