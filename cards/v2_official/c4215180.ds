card "Lollipo☆Yummy" {
    id: 4215180
    type: Effect Monster
    attribute: LIGHT
    race: Beast
    level: 1
    atk: 600
    def: 1200

    effect "Effect 2" {
        speed: 1
        once_per_turn: soft
        timing: if
        trigger: summoned
        resolve {
            return (all, card, either controls) to deck
            banish (all, card, either controls)
        }
    }

    effect "Effect 3" {
        speed: 1
        once_per_turn: soft
        timing: if
        trigger: summoned by special
        resolve {
            return (all, card, either controls) to deck
            banish (all, card, either controls)
        }
    }
}
