card "Recycle" {
    id: 96316857
    type: Continuous Spell

    effect "Effect 1" {
        speed: 1
        mandatory
    }

    effect "Effect 2" {
        speed: 1
        once_per_turn: soft
        timing: when
        trigger: standby_phase
        cost {
            pay_lp 300
        }
        resolve {
            return (all, card, either controls) to deck
        }
    }
}
