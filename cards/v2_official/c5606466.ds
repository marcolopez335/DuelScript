card "D.D. Trap Hole" {
    id: 5606466
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            destroy (all, card, either controls)
        }
    }

    effect "Effect 2" {
        speed: 2
        mandatory
        resolve {
            destroy (all, card, either controls)
        }
    }

    effect "Effect 3" {
        speed: 2
        mandatory
        trigger: summoned by special
        resolve {
            destroy (all, card, either controls)
        }
    }
}
