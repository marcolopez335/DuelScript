card "Noble Knights of the Round Table" {
    id: 55742055
    type: Field Spell

    effect "Effect 1" {
        speed: 1
        mandatory
    }

    effect "Effect 2" {
        speed: 1
        timing: when
        trigger: end_phase
        resolve {
            send (all, card, either controls) to gy
            special_summon (all, card, either controls)
            add_to_hand (all, card, either controls)
            draw 1
        }
    }
}
