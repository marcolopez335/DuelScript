card "The Selection" {
    id: 30888983
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        cost {
            pay_lp 1000
        }
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }

    effect "Effect 2" {
        speed: 2
        mandatory
        cost {
            pay_lp 1000
        }
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }

    effect "Effect 3" {
        speed: 2
        mandatory
        cost {
            pay_lp 1000
        }
        resolve {
            negate
            destroy (all, card, either controls)
        }
    }
}
