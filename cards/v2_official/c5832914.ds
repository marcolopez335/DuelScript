card "Toon Briefcase" {
    id: 5832914
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        trigger: summoned
        resolve {
            return (all, card, either controls) to deck
        }
    }

    effect "Effect 2" {
        speed: 2
        mandatory
        trigger: summoned by flip
        resolve {
            return (all, card, either controls) to deck
        }
    }

    effect "Effect 3" {
        speed: 2
        mandatory
        trigger: summoned by special
        resolve {
            return (all, card, either controls) to deck
        }
    }
}
