card "Gambler of Legend" {
    id: 2196767
    type: Effect Monster
    attribute: DARK
    race: Spellcaster
    level: 4
    atk: 500
    def: 1400

    effect "Effect 1" {
        speed: 1
        once_per_turn: soft
        mandatory
        resolve {
            destroy (all, card, either controls)
            send (all, card, either controls) to gy
            destroy (all, card, either controls)
            send (all, card, either controls) to gy
        }
    }
}
