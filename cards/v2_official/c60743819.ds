card "Fiend Griefing" {
    id: 60743819
    type: Normal Trap

    effect "Effect 1" {
        speed: 2
        mandatory
        resolve {
            return (all, card, either controls) to deck
            send (all, card, either controls) to gy
        }
    }
}
