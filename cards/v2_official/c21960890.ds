card "Dark Necromancer" {
    id: 21960890
    type: Effect Monster
    attribute: DARK
    race: Zombie
    level: 4
    atk: 1000
    def: 1400

    effect "Effect 1" {
        speed: 1
        once_per_turn: hard
        timing: if
        trigger: summoned
        resolve {
            send (all, card, either controls) to gy
            add_to_hand (all, card, either controls)
        }
    }

    effect "Effect 2" {
        speed: 1
        once_per_turn: hard
        timing: if
        trigger: summoned by special
        resolve {
            send (all, card, either controls) to gy
            add_to_hand (all, card, either controls)
        }
    }
}
